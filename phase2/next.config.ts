import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["via.placeholder.com", "source.unsplash.com", "picsum.photos","preview.redd.it"],
  }
};

export default nextConfig;
